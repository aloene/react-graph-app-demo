import React, { FC, Fragment } from 'react';

const LoginPage: FC = () => {
    return (
    <Fragment>
        <h1>Loging in using requested provider...</h1>
    </Fragment>)
};

export default LoginPage;
